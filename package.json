{
  "name": "link-saver-take-home",
  "version": "1.0.0",
  "private": true,
  "description": "A small persistent link saver built through a structured AI-assisted workflow.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.1.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
